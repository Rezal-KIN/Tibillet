{
  "slug": "gala-am-aix-2027",
  "platform": "v1",
  "deployed_release_id": "gala-am-aix-2027-v1.0.0",
  "db_size_estimate_mb": 850,
  "backup_retention_days": 90,
  "status": "preparation",
  "deployment_locked": false,
  "notes": "Fields here are exactly what Terraform cannot derive: platform choice, which release is deployed, retention policy, and lifecycle status (preparation, live, frozen, dormant, archived). Instance ID, public IP, runtime secret ARN, and backup prefix come from `terraform output galas` instead of being duplicated here — run that command rather than trusting a stale copy."
}
